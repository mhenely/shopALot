MONGODB_URI=
TEST_MONGODB_URI=
PORT=3001
SECRET=

# Stripe (test mode) — checkout
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
# Where to send the user back after Stripe checkout (the frontend origin)
FRONTEND_URL=http://localhost:5173

# Resend — order confirmation email
RESEND_API_KEY=
# Must be an address on a domain verified in Resend, e.g. "shopALot <orders@matthenely.com>"
FROM_EMAIL=
# Link to your hosted resume (shown in the confirmation email). Optional.
RESUME_URL=

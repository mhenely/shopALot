# assets

Drop your resume here as **`resume.pdf`** (exact filename).

The order-confirmation email (`utils/email.js`) attaches it as `Matt-Henely-Resume.pdf`.
If the file is missing, the email still sends — just without the attachment — so this is
optional but recommended.

```
backend/assets/resume.pdf   <-- add this file
```
